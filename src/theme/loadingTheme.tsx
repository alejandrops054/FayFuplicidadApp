import {StyleSheet} from 'react-native';

export const sytyleLoading = StyleSheet.create({
  View: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
  },

  ActivityIn: {},
});
